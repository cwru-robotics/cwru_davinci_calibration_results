cmake_minimum_required(VERSION 2.8.3)
project(cwru_davinci_calibration_results)

find_package(catkin REQUIRED COMPONENTS)

catkin_package(
  # INCLUDE_DIRS
	# DEPENDS
	# LIBRARIES
	# CATKIN_DEPENDS
)
